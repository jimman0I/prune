// Temporary: the generated file lists every language. Only `el` exists yet.
import el from './el.js';

/** Deep Clean's rule and category text per language, keyed like catalog.js.
 * English is not here -- it is the rules file itself. */
export const CLEANER_TEXT = { el };
